import { FormateDataPipe } from './formate-data-pipe';

describe('FormateDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateDataPipe();
    expect(pipe).toBeTruthy();
  });
});
