import { TypeFormatePipe } from './type-formate-pipe';

describe('TypeFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
